import scrollphathd
from scrollphathd.fonts import font3x5
#This overwrites any current text on screen and then clears once script finishes
scrollphathd.write_string('DONE',font=font3x5,brightness=0.5)
scrollphathd.show()
scrollphathd.clear()
